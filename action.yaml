name: Custom Action

description: Greet someone

inputs:
  who-to-greet:
    description: Who to greet
    required: true
    default: Hello

outputs:
  time:
    description: time greeted

runs:
  using: node24
  main: dist/index.js